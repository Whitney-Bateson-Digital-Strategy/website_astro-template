---
// TODO: Build out related posts display when blog section is developed.
// Previously depended on widgets/BlogHighlightedPosts.astro (removed).
import type { Post } from '~/types';

export interface Props {
  post: Post;
}
---
